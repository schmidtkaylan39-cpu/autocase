# Release v0.1.0

Release date: 2026-04-18 (Asia/Shanghai)

## Release Focus

This promotion marks the default operating route as GPT-5.4 + Codex:

- orchestrator default runtime is now manual (GPT-5.4-led), not OpenClaw-first
- executor/verifier defaults remain `codex` and `local-ci`
- OpenClaw and Cursor remain available as optional opt-in surfaces
- runtime doctor now reports required vs optional readiness classes for the default route

## Key Delivery Changes

- switched orchestrator runtime preference to manual default
- switched orchestrator default model policy to `gpt-5.4`
- updated role/config defaults to "GPT-5.4 + Codex (manual orchestration)"
- aligned architecture, handoff, model-routing, runtime-doctor, and release-readiness docs
- retained full OpenClaw launcher/dispatch compatibility for explicit opt-in routing

## Validation Evidence

The following checks were passed during release promotion:

- `npm run selfcheck`
- `npm run backup:project -- --output-dir reports/release-readiness/backup-smoke-5db3fc4`
- `npm run release:win -- --output-dir reports/release-readiness/windows-release-smoke-5db3fc4`

CI evidence:

- GitHub Actions run `24594021904` (all jobs passed):  
  https://github.com/schmidtkaylan39-cpu/autocase/actions/runs/24594021904

Promotion commit lineage:

- baseline candidate commit: `5db3fc4`
- mainline promotion merge: `307e450`
