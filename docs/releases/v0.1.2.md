# Release v0.1.2

Release date: 2026-04-18 (Asia/Shanghai)

## Release Focus

This promotion hardens release evidence integrity and review-bundle contract coverage.

- keeps the default GPT-5.4 + Codex route unchanged
- adds stricter bundle referential-integrity assertions
- formalizes published release evidence as a reproducible in-repo artifact

## Key Changes

- strengthened `review-bundle` tests to verify references for:
  - `manifest.paths`
  - `manifest.evidence.reportFiles`
  - `manifest.evidence.runs[].reportPath` and `manifest.evidence.runs[].runStatePath`
  - `metadata/validation-results.json` evidence paths
- added release publish evidence generator:
  - `scripts/release-publish-evidence.mjs`
  - `npm run release:evidence`
- added release evidence contract tests:
  - `tests/release-publish-evidence-tests.mjs`
- persisted a formal release evidence artifact for `v0.1.1`:
  - `docs/releases/v0.1.1.evidence.json`

## External Review Outcome

External reviewer result for bundle built from `a5193f3`:

- no new concrete bug found
- no new state-transition correctness break
- no manual/hybrid follow-up safety break
- no cross-platform archive regression
- no runtime/model routing drift against documented baseline
- no review-bundle evidence-path break

Reviewer rerun checks from unpacked `repo/` (with `npm ci` first) all passed:

- `npm ci`
- `npm test`
- `npm run validate:workflows`
- `npm run pack:check`

## Validation Evidence

Promotion validation for this line:

- `npm run build`
- `npm run lint`
- `npm run typecheck`
- `npm run validate:workflows`
- `npm run pack:check`
- `npm test`
- `npm run selfcheck`
- machine-readable published release evidence:
  - `docs/releases/v0.1.2.evidence.json`

Release review artifacts prepared:

- `external-ai-review-20260418-a5193f3-gpt-slim.zip`
- `gpt-review-prompt-post-v0.1.1-a5193f3.md`

## Residual Risks (Non-Blocking)

- Windows release/backup path is still a newer surface than dispatch/run-state.
- Bundle rerun still depends on environment setup (`npm ci`), by design.
- Non-default runtime surfaces remain valid but less exercised than the default route.

## Promotion Lineage

- previous release tag: `v0.1.1`
- hardening candidate reviewed externally: `a5193f3`
- release tag for this promotion: `v0.1.2`
